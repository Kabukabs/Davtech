[
  {
    "origin": ["http://localhost:5174"], //Replace 'http://localhost:5174' with the developer's own localhost URL if different.
    "method": ["GET", "POST", "PUT"],
    "maxAgeSeconds": 3600
  }
]
 